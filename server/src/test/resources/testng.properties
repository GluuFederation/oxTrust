# Login as user
test.login.user.admin.uid=${user.admin.uid}

# Login as user
test.login.user.generic.uid=${user.generic.uid}
test.login.user.generic.password=${user.generic.password}

# Group
test.group.dn=inum=${user.generic.group.member.inum},ou=groups,o=gluu
test.group.dn.ownerDn=inum=${group.owner.user.inum},ou=people,o=gluu
test.group.dn.nonMemberDn=inum=${group.non_member.user.inum},ou=people,o=gluu

# Person
person.search.pattern=${user.generic.uid}
person.uid=${user.generic.uid}

test.scim2.userext.create_json=${scim2.userext.create_json}

test.keep.persons=${test.keep.persons}
test.keep.groups=${test.keep.groups}
